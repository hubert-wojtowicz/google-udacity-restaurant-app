# node packages
node_modules/

# grunt resized pictures
img-resized/

# those files will not be tracked as long as they can be generated with build process
build/

package-lock.json

# file where tokens and api key are stored
config.js